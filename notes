git init 
    - initializes the working directory (starts tracking any changes)
git status 
    - shows the staging area
git add [file_name]
    - moves the specified file to staging area
git commit
    - commits the changes
git commit -m "Hello"
    - Commits the changes with a `Hello` message
git log
    - Can see the previous commits made
git diff [file_name]
    - shows the difference before and after the file modification
git checkout [file_name]
    - rollsback to the last version(unmodified version of the file)
git remote add [remote_repository_name] [URL] #origin is the most common remote repository name, URL of the remote repo on github
    - adds the remote repo
git push -u [remote_repository_name] [branch_name]
    - Uploads the local repository to the remote repository on github
git branch
    - gives list of branches
git branch [name]
    - creates a new branch names as [name]
git checkout [branch_name]
    - switches to the specified branch
git rm --cached -r [file_name]
    - removes specified files from staging area